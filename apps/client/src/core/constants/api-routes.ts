const API_ROUTES = {
  example: '/example',
  auth: {
    login: '/login',
    signup: '/register',
    logout: '/logout',
    refresh: '/refresh',
    google: '/google'
  }
};

export default API_ROUTES;
