const API_ROUTES = {
  example: '/example',
  auth: {
    login: '/auth/login',
    signup: '/auth/register',
    logout: '/auth/logout',
    refresh: '/auth/refresh',
    google: '/auth/google'
  }
};

export default API_ROUTES;
