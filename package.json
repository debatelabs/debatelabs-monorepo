{
  "name": "debatelabs",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "dev:client": "turbo dev --filter=@debatelabs/client",
    "dev:server": "turbo dev --filter=@debatelabs/server",
    "test": "turbo test",
    "test:client": "turbo test --filter=@debatelabs/client",
    "test:server": "turbo test --filter=@debatelabs/server",
    "test:related": "turbo test:related",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,md}\"",
    "prepare": "husky install",
    "prisma:generate": "pnpm --filter=@repo/server generate"
  },
  "devDependencies": {
    "eslint": "^9.15.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.4.1",
    "prettier": "^3.2.5",
    "turbo": "^2.3.3",
    "typescript": "^5.5.4"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=18"
  }
}
